<!DOCTYPE html>
<html>
<head>
  <title>Gym Connection App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>
  
  <div class="container mt-4">
    <h1>Welcome to Gym Connection</h1>
    <% if (!user) { %>
      <div class="row mt-4">
        <div class="col-md-6">
          <h2>For Students</h2>
          <p>Find the perfect gym and instructor for your fitness journey.</p>
          <a href="/auth/signup/student" class="btn btn-primary">Sign Up as Student</a>
        </div>
        <div class="col-md-6">
          <h2>For Instructors</h2>
          <p>Connect with students and manage your gym listings.</p>
          <a href="/auth/signup/instructor" class="btn btn-success">Sign Up as Instructor</a>
        </div>
      </div>
    <% } else { %>
      <p>Welcome back, <%= user.fullName %>!</p>
      <% if (user.type === 'instructor') { %>
        <a href="/gyms/add" class="btn btn-primary">Add New Gym</a>
      <% } %>
      <a href="/gyms" class="btn btn-info">View Gyms</a>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>